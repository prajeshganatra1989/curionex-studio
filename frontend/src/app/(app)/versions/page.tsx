import { ComingSoonPage } from "@/components/layout/coming-soon";

export const metadata = { title: "Versions" };

export default function VersionsPage() {
  return (
    <ComingSoonPage
      title="Versions"
      description="Immutable content versions will be browsable here next."
    />
  );
}
