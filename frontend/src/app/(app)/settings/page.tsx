import { ComingSoonPage } from "@/components/layout/coming-soon";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <ComingSoonPage
      title="Settings"
      description="Account and studio preferences will land here later."
    />
  );
}
