import { ProjectSectionPlaceholder } from "@/components/projects/project-home-page";

export const metadata = { title: "Versions" };

export default function ProjectVersionsPage() {
  return (
    <ProjectSectionPlaceholder
      title="Versions"
      description="Project version browsing will expand here. Overview already shows latest and approved summaries."
    />
  );
}
