import { ComingSoonPage } from "@/components/layout/coming-soon";

export const metadata = { title: "Projects" };

export default function ProjectsPage() {
  return (
    <ComingSoonPage
      title="Projects"
      description="Project management UI arrives in the next frontend sprint."
    />
  );
}
