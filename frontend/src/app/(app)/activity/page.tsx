import { ComingSoonPage } from "@/components/layout/coming-soon";

export const metadata = { title: "Activity" };

export default function ActivityPage() {
  return (
    <ComingSoonPage
      title="Activity"
      description="Full audit timeline UI is planned for a future sprint."
    />
  );
}
